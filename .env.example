# The only GitHub account this widget will ever serve stats for.
GITHUB_USERNAME=

# GitHub personal access token with `read:user` and `repo` (read) scopes.
# Used to query the GitHub GraphQL API for stats.
GITHUB_TOKEN=
